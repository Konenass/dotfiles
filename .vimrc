runtime! archlinux.vim

" Let vim recognize the type of file in use
filetype on

" Turn on Syntax highlighting
syntax on

" Display line number on the side
set number

" Search configuration
set incsearch
set ignorecase
set hlsearch

" Theme
set background=dark
colorscheme gruvbox
